# EmailJS (optional – for contact form to send email from the site)
# Get these from https://www.emailjs.com/ after creating a service and template.
# If not set, the contact form will open the user's email client (mailto) instead.
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id
VITE_EMAILJS_PUBLIC_KEY=your_public_key
